import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function run() { const role = await prisma.role.findFirst({ where: { name: 'IT_Admin' } }); if (!role) { await prisma.role.create({ data: { id: 1, name: 'IT_Admin', templateId: 1 } }).catch(e => console.log('Failed to create role 1', e)); console.log('Role IT_Admin created or attempted.'); } else { console.log('Role IT_Admin exists with id:', role.id); } } run().finally(() => prisma.$disconnect());
